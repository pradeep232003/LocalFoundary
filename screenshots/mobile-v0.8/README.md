# Mobile UI checks · v0.8.0

Production React bundle with deterministic API fixtures. These screenshots demonstrate UI states; they are not native APK/IPA build evidence.

Verified: required-field/network consent gating, Windows IPA explanation, APK request payload, progress polling, ready download and authenticated download request; Mac IPA build payload and download controls. Axe checked the Mobile panel for WCAG 2.1 A/AA serious or critical findings.
